{
  "name": "query-benchmarks",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "engines": {
    "node": ">=20 <25"
  },
  "scripts": {
    "test": "vitest run --config vitest.config.ts",
    "bench": "node --env-file=.env.preview ./node_modules/vitest/vitest.mjs run --config vitest.config.ts",
    "typecheck": "tsc --project tsconfig.json --noEmit"
  },
  "dependencies": {
    "@514labs/moose-lib": "link:../node_modules/@514labs/moose-lib"
  },
  "devDependencies": {
    "vitest": "^3.2.4",
    "@types/node": "^20.19.33",
    "typescript": "^5.7.0"
  }
}
