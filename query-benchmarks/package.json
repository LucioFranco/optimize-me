{
  "name": "query-benchmarks",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "engines": {
    "node": ">=20 <25"
  },
  "scripts": {
    "test": "vitest run --config vitest.config.ts",
    "bench": "node --env-file=.env.preview ./node_modules/vitest/vitest.mjs run --config vitest.config.ts",
    "typecheck": "tsc --project tsconfig.json --noEmit"
  }
}
